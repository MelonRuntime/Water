{
  "scripts": {
    "build": "babel --extensions .ts ./src/ --out-dir ./babel && webpack ./babel/index.js",
    "go": "npm run build && npx melon load ."
  },
  "dependencies": {
  },
  "devDependencies": {
    "@babel/cli": "latest",
    "@babel/core": "latest",
    "@babel/preset-env": "latest",
    "@babel/preset-typescript": "latest",
    "melon-types": "latest",
    "webpack-cli": "latest"
  }
}